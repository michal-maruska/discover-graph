# Build on top of petgraph

https://github.com/petgraph/petgraph

For a graph that is (really) discovered dynamically, grows as needed.

We need:
- dynamic_bit_set
- dynamic_graph

- plus the algorithm to discover the graph...

